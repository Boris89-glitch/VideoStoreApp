@model Video_store_app.ViewModels.RandomMovieViewModel
@{
    ViewBag.Title = "Random";
}

<h2>@Model.Movie.Name</h2>
@if (Model.Customers.Count == 0)
{
    <p>Noone has ranted this movie before!</p>
}
    <ul>
        @foreach (var customer in Model.Customers)
        {
            <li>@customer.Name</li>
        }

    </ul>

